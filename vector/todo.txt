Add the following tests:
    error handling

Add to Vector:
    Add functionality to iterator to make it random access instead of bidirectional
    Add erase function
    Add insert function
    Add function to give the pointer to the vector's underlying array (maybe)
